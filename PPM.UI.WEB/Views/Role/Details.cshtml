@model PPM.DAL.Models.Role

@{
    ViewBag.Title = "Details";
}

<h2 style="color: orange;"><u>Role Details</u></h2>
<br>

@if (TempData["ErrorMessage"] != null)
    {
        <div class = "alert alert-danger">
            <strong>Error ! </strong>@TempData["ErrorMessage"]
            <button type="button" class="close" data-dis="alert" aria-label="Close">
                <span>&times;</span>
            </button>
         </div>
    }

<div>
    <h4>Role</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RoleId)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.RoleId)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.RoleName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.RoleName)
        </dd>
    </dl>
</div>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.RoleId }, new {@class = "btn btn-primary"}) &nbsp;
    @Html.ActionLink("Delete", "Delete", new { id = Model.RoleId }, new {@class = "btn btn-danger"})
</p>
<br>
<div>
   <h6>@Html.ActionLink("Back to List", "Index")</h6>
</div>
